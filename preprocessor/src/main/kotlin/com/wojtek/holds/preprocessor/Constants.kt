package com.wojtek.holds.preprocessor

/**
 * Preprocessor constants.
 */
object Constants {
    /**
     * Default version to use when generating hold configurations.
     *
     * Change this value when you want to generate a new wall version.
     * This should match the DEFAULT_VERSION in the web app.
     */
    const val DEFAULT_VERSION = "v1"
}
